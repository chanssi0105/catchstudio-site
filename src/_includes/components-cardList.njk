<a class="p-card" href="{{ p.url }}">
  <img class="p-thumb" src="{{ p.data.thumb }}" alt="{{ p.data.title }}" loading="lazy" decoding="async">

  {% if p.data.category %}
    <span class="p-cat">{{ p.data.category }}</span>
  {% endif %}

  <span class="p-title">{{ p.data.title }}</span>
</a>
