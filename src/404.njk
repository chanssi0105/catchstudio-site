---
layout: layouts/base.njk
title: "404 | Catch Studio"
description: "요청하신 페이지를 찾을 수 없습니다."
canonical: "https://catch-studio.com/404.html"
ogImage: "https://catch-studio.com/assets/images/og/home.jpg"
permalink: "404.html"
---
<style>
  .coming{display:flex; flex-direction:column; justify-content:center; align-items:center; width:100%; height:100vh; text-align:center;}
  .coming p{margin-top:1rem;}
  .btn{margin-top:2rem;}
  .btn a{display:inline-block; width:140px; padding:10px 0; color:#fff; font-weight:700; text-align:center; border-radius:100px; background-color:#000;}
  .req{margin-top:1rem; font-size:0.95rem; opacity:0.75;}
</style>

<div class="coming">
  <h1>404 ERROR</h1>

  <p>
    죄송합니다. 페이지를 찾을 수 없습니다.<br />
    존재하지 않는 주소를 입력하셨거나<br />
    요청하신 페이지의 주소가 변경, 삭제되어 찾을 수 없습니다.
  </p>

  <p class="req">
    요청 경로: <span id="req"></span>
  </p>

  <div class="btn">
    <a href="/">홈으로</a>
  </div>
</div>

<script>
  // 사용자가 요청한 경로를 보여주기
  (function () {
    var target = document.getElementById('req');
    if (!target) return;
    try {
      var path = window.location.pathname + window.location.search;
      if (path && path.length > 1) target.textContent = decodeURI(path);
      else target.textContent = "/";
    } catch (e) {
      target.textContent = "";
    }
  })();
</script>
